import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// No need to wrap your app component around a <Provider> ! Render as usual
ReactDOM.render(<App />, document.getElementById('root'));